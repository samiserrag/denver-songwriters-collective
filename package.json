{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:sql": "vitest run tests/sql/",
    "test:rpc": "vitest run tests/rpc/",
    "test:hooks": "vitest run tests/hooks/",
    "test:components": "vitest run tests/components/",
    "test:e2e": "vitest run tests/end-to-end/",
    "test:utils": "vitest run tests/utils/",
    "test:concurrency": "vitest run tests/rpc/concurrency.test.ts",
    "test:rls": "vitest run tests/sql/rls-policies.test.ts",
    "test:triggers": "vitest run tests/sql/triggers.test.ts",
    "test:errors": "vitest run tests/rpc/error-flows.test.ts",
    "test:validate-env": "npx tsx tests/utils/validateTestEnvironment.ts",
    "test:ci": "vitest run --reporter=verbose --no-color",
    "test:all": "npm run test:validate-env && npm run test"
  },
  "dependencies": {
    "@supabase/ssr": "^0.1.0",
    "@supabase/supabase-js": "^2.84.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@vitejs/plugin-react": "^4.2.0",
    "@testing-library/react": "^14.2.0",
    "@testing-library/jest-dom": "^6.4.0",
    "@testing-library/user-event": "^14.5.0",
    "@vitest/coverage-v8": "^1.6.0",
    "@vitest/ui": "^1.6.0",
    "jsdom": "^24.0.0",
    "msw": "^2.2.0",
    "@faker-js/faker": "^8.4.0",
    "tsx": "^4.7.0",
    "dotenv": "^16.4.0"
  }
}
